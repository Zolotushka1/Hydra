# Values this repository contains on purpose, each with the reason it is here.
#
# Exact strings only. Allowing a fixture must not accidentally allow a real
# secret that resembles it, so there are no patterns in this file.
#
# Adding an entry is a decision about a specific value. Widening
# scripts/secret-canaries.txt to make a finding disappear is not the same thing
# and is not the fix.

# The x25519 public key from verifying our Reality key generation against
# `xray x25519`. A published test vector, not material for any deployment.
NhUhziBHN2ePb3z3bp4eY1fzGDceZG_GVMav_avdul4

# Four fixtures that must contain PEM-shaped text for their tests to mean
# anything: each one asserts that the panel redacts a private key out of node
# reports, provisioning step output and handoff detail. Remove the fixture and
# the redaction test passes on an empty string.
"-----BEGIN OPENSSH PRIVATE KEY-----\nsecret\n-----END OPENSSH PRIVATE KEY-----";
"-----BEGIN OPENSSH PRIVATE KEY-----\n{}\n-----END OPENSSH PRIVATE KEY-----",
"TOKEN=secret-value\n-----BEGIN OPENSSH PRIVATE KEY-----\n{}\n-----END OPENSSH PRIVATE KEY-----",

# The secret guard's own negative control: it feeds itself a PEM header and
# asserts it is rejected. Without this line that guard is untested.
let leaked = serde_json::json!({ "detail": "-----BEGIN PRIVATE KEY-----" });

# The secret guard's other negative control: base64 text shaped like a bearer
# token, fed to the guard to prove it rejects one.
Zm9vYmFyMTIzNDU2Nzg5MEFCQ0RFRkdI

# Documentation placeholders. A credential_ref is a reference by definition —
# the panel-node contract forbids it from carrying key material — and the
# challenge_token line describes the field rather than showing a value.
cluster/cluster-1/node/relay-1/mtls
optional-for-2fa-2step
